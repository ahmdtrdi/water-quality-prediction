
USE ROLE ACCOUNTADMIN;
USE DATABASE SNOWFLAKE_LEARNING_DB;
USE SCHEMA PUBLIC;

CREATE OR REPLACE SECRET GITHUB_PAT_SECRET
  TYPE = PASSWORD
  USERNAME = '' 
  PASSWORD = '';

ALTER API INTEGRATION GITHUB
  SET ALLOWED_AUTHENTICATION_SECRETS = ('');

ALTER GIT REPOSITORY EY_PROJECT_REPO
  SET GIT_CREDENTIALS = ;