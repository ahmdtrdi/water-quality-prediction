# Feature Sets Configuration
# Generated by 03_feature_engineering.ipynb after RFECV/Boruta selection
# Each target gets its own optimized feature set

alkalinity:
  description: "Features selected for Total Alkalinity prediction"
  features: []  # Populated after feature selection in notebook 03

ec:
  description: "Features selected for Electrical Conductance prediction"
  features: []  # Populated after feature selection in notebook 03

drp:
  description: "Features selected for Dissolved Reactive Phosphorus prediction"
  features: []  # Populated after feature selection in notebook 03

# Common features used across all targets (intersection)
common:
  description: "Features that appear in all 3 target-specific sets"
  features: []
